#include <iostream>
using namespace std;
int main ()
{
	int i,n,key,a[10],count=10;
	cout<<n<<key;
	for (i=0;i<n;i++)
	{
		cin>>a[i]
		if (a[i]==key)
		cout<<"sk found at "<<i++;
		count++;
		
	}
	if (count==0)
	cout<<"key not found ";
	else
	cout << sk<< "found "<<cout<<"time";

};